# BarcodeSDK 使用说明文档
## 1. 项目概述
BarcodeSDK 是跨平台鱼眼相机 AprilTag / Aruco 阵列检测库，支持 Windows x64 / Linux ARM64(Docker无头环境)，提供鱼眼畸变校正、USB相机采集、视频文件解析、标记板位姿解算全套能力。
- 支持标记类型：Aruco 标准码、Aruco MIP 码、AprilTag 36H11
- 支持载体：实时USB摄像头、本地mp4视频文件
- 输出：相机相对于标记阵列的世界坐标(X/Y/Z 米)、单帧检测耗时
- 编译模式：静态库 / 动态库自由切换，自带Demo演示程序

## 2. 环境与依赖
### 2.1 编译依赖（SDK已内置，使用者无需自行编译）
- CMake ≥ 3.18
- C++17
- OpenCV 4.12.0 (含contrib)
- VTK 9.2.6
- aruco 3.1.12
- apriltag
### 2.2 运行环境
- Windows：VS2017+ x64，运行库 `/MD`(Release) / `/MDd`(Debug)
- Linux：ARM64 Ubuntu/Docker，支持无头无界面运行

## 3. SDK 目录结构
```
SDK_Win64 / SDK_ARM64
├─ bin/                动态库(dll/so) + Demo可执行文件 + 第三方运行时库
├─ lib/                静态库(a/lib)、导入库
├─ include/            SDK对外头文件
├─ third_party/        全套预编译第三方依赖
└─ lib/cmake/BarcodeSDK CMake包配置文件，外部find_package加载
```

## 4. 源码编译参数说明
### 4.1 编译开关（cmake -DXXX=ON/OFF）
| 参数 | 取值 | 说明 |
|------|------|------|
| BUILD_SHARED_LIBS | ON/OFF | ON=编译动态库(dll/so)；OFF=编译静态库(lib/a) |
| BUILD_DEMO | ON/OFF | 是否编译main.cpp演示程序BarcodeDemo |
| DOCKER_BUILD | ON/OFF | Linux专属，ON启用无头模式(关闭窗口、imshow) |
| CMAKE_INSTALL_PREFIX | 路径 | 指定SDK打包输出目录 |

### 4.2 Windows 编译命令示例
```bash
# 编译动态库Release，输出SDK_Win64
cmake -B build_win64 -DCMAKE_INSTALL_PREFIX=./SDK_Win64 -DBUILD_SHARED_LIBS=ON
cmake --build build_win64 --config Release
cmake --install build_win64 --config Release

# 编译静态库Debug
cmake -B build_win64_debug -DCMAKE_INSTALL_PREFIX=./SDK_Win64_Debug -DBUILD_SHARED_LIBS=OFF
cmake --build build_win64_debug --config Debug
cmake --install build_win64_debug --config Debug
```

### 4.3 Linux ARM64 编译命令示例
```bash
# 静态库 + Docker无头模式
cmake -B build_arm64 -DCMAKE_INSTALL_PREFIX=./SDK_ARM64 -DBUILD_SHARED_LIBS=OFF -DDOCKER_BUILD=ON
cmake --build build_arm64
cmake --install build_arm64
```

### 4.4 外部项目集成 CMake 示例
```cmake
cmake_minimum_required(VERSION 3.18)
project(ClientTest)
set(CMAKE_CXX_STANDARD 17)

# 替换为你的SDK解压路径
set(BarcodeSDK_DIR "/xxx/SDK_Win64/lib/cmake/BarcodeSDK")
find_package(BarcodeSDK REQUIRED)

add_executable(main main.cpp)
target_link_libraries(main PRIVATE BarcodeSDK::BarcodeSDK)
```

## 5. 标定文件 fisheye_calib.xml 格式说明
### 5.1 文件作用
存储鱼眼相机内参、畸变系数、图像分辨率，用于实时画面去畸变校正。
### 5.2 xml关键字段说明
```xml
<?xml version="1.0"?>
<opencv_storage>
<!-- 相机内参矩阵 K 3x3 -->
<cameraMatrix type_id="opencv-matrix">
  <rows>3</rows>
  <cols>3</cols>
  <dt>d</dt>
  <data>fx 0 cx 0 fy cy 0 0 1.</data>
</cameraMatrix>
<!-- 鱼眼畸变系数 D 4x1 -->
<distCoeffs type_id="opencv-matrix">
  <rows>4</rows>
  <cols>1</cols>
  <dt>d</dt>
  <data>k1 k2 k3 k4</data>
</distCoeffs>
<!-- 采集图像宽高 -->
<imageSize>640 480</imageSize>
</opencv_storage>
```
- fx/fy：相机焦距(像素)
- cx/cy：图像主点(光心像素坐标)
- k1/k2/k3/k4：鱼眼径向畸变系数
- imageSize：标定采集图像分辨率，相机采集分辨率必须与该值一致

### 5.3 标定生成方式
调用库内标定函数 `runFisheyeCalib(图片文件夹路径, 输出xml路径)`，拍摄多张棋盘格鱼眼图像自动生成标定文件。

## 6. 相机参数配置说明
### 6.1 设备选择
- Windows：使用 `CAP_DSHOW` 打开USB摄像头，设备号 0/1/2 依次尝试
- Linux：自动遍历 `/dev/video0~32`，仅筛选 `uvcvideo` USB相机，跳过MIPI虚拟设备

### 6.2 核心相机参数
1. 分辨率
    必须与标定文件 `imageSize` 宽高保持一致，否则校正失效
    ```cpp
    cap.set(cv::CAP_PROP_FRAME_WIDTH, width);
    cap.set(cv::CAP_PROP_FRAME_HEIGHT, height);
    ```
2. 缓冲队列
    ```cpp
    cap.set(cv::CAP_PROP_BUFFERSIZE, 1);
    ```
    缩小相机缓存，降低画面延迟，实时检测场景必开
3. 帧率控制
    循环内 `waitKey(20)` 限制最大50FPS，降低CPU占用
4. 取帧优化
    使用 `grab() + retrieve()` 替代 `read()`，丢弃缓存旧帧，保证画面实时性

### 6.3 相机打开失败排查
1. 设备号错误，更换0/1/2重试
2. 分辨率与标定文件不匹配
3. Linux权限不足：`sudo chmod 666 /dev/video*`
4. Docker环境未挂载设备：启动命令添加 `--device /dev/video0`

## 7. Tag 标记板尺寸配置
### 7.1 关键参数定义
```cpp
// 标记板行列
int rows = 4;
int cols = 5;
// 单个Tag物理边长（单位：米）
double tagSize = 0.02255;
// 相邻Tag中心间距（X/Y方向，单位：米）
double tagGapX = 0.0451;
double tagGapY = 0.0451;
```
### 7.2 参数含义
1. `rows / cols`：标记阵列行数、列数（示例4行5列）
2. `tagSize`：单个二维码正方形边长，单位米，打印实物时用尺子实测填写
3. `tagGapX / tagGapY`：两个相邻标记中心点之间的距离，实物测量
4. `startTagValue`：阵列起始ID
   - Aruco系列：起始ID=0
   - AprilTag 36H11：起始ID=560

### 7.3 标记类型选择
```cpp
TagType::ARUCO_MOD    // OpenCV标准Aruco码
TagType::ARUCO_TAG    // aruco库MIP高精度码
TagType::APRIL_TAG    // AprilTag 36H11标记
```
### 7.4 字典参数
- Aruco标准字典：`cv::aruco::DICT_4X4_50`
- Aruco MIP字典：`aruco::Dictionary::ARUCO_MIP_36h12`
- AprilTag家族：`AprilTagFamily::TAG_36H11`

## 8. Demo 使用操作指南
### 8.1 实时相机检测模式 runTagDetectRealTime
1. 放置 `fisheye_calib.xml` 在程序上级目录
2. 运行 BarcodeDemo
3. 输出控制台：相机世界坐标X/Y/Z、单帧校正+检测耗时
4. 按键 `q / ESC` 退出程序
5. Docker无头环境：无图像窗口，仅控制台输出位姿

### 8.2 视频文件解析模式 runAprilDetectDemo
1. 新建 `mp4` 文件夹，放入录制视频
2. 标定文件同上，程序自动读取视频逐帧检测
3. 按键 `q` 中途退出解析

### 8.3 视频录制模式 recordVideoFromCamera
1. 执行后预览相机画面
2. 按键 `b` 开始录制mp4视频
3. 按键 `q` 停止录制并保存文件

## 9. 运行依赖问题解决
### Windows
1. 将 `SDK/bin` 全部dll复制到exe同目录；或配置VS调试环境PATH
2. Release/Debug运行库必须匹配：程序/SDK统一 `/MD` 或 `/MDd`

### Linux
1. 临时加载so库：`export LD_LIBRARY_PATH=./bin:$LD_LIBRARY_PATH`
2. Docker无头编译必须开启 `-DDOCKER_BUILD=ON`，否则UI库报错

## 10. 接口快速上手示例
```cpp
#include "FisheyeCalib.h"
#include "AprilTagJointDetect.h"
#include "TagJointDetect.h"

// 1. 加载鱼眼标定
FisheyeUndist undist;
undist.init("../fisheye_calib.xml");
cv::Mat K = undist.getCameraMatrix();
cv::Mat D = undist.getDistCoeffs();

// 2. 初始化AprilTag检测器
std::shared_ptr<TagJointDetect> detector = std::make_shared<AprilTagJointDetect>(AprilTagFamily::TAG_36H11, 0.02255, 4,5);
detector->setCameraParam(K,D);
detector->setTagDist(0.0451,0.0451);

// 3. 帧去畸变+检测
cv::Mat frameFix = undist.undistortImage(rawFrame);
BoardResult res = detector->detect(frameFix);
if(res.board_pose_valid){
    // 相机光心世界坐标 X/Y/Z
    double x = res.camera_tvec[0];
    double y = res.camera_tvec[1];
    double z = res.camera_tvec[2];
}
```

## 11. 版本与更新说明
- SDK版本：1.0
- 支持平台：Windows x64、Linux ARM64
- 编译标准：C++17
- 第三方库固定版本：OpenCV4.12.0 / VTK9.2.6 / aruco3.1.12
- 更新日志：
  1. 支持Docker无头无界面运行
  2. 相机缓冲优化降低延迟
  3. 分平台自动第三方依赖路径
  4. CMake Install一键打包完整SDK